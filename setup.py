# setup.py

from setuptools import setup, find_packages

setup(
    name='safe-bool',
    version='0.1',
    description='Safe boolean conversion utility',
    author='Your Name',
    packages=find_packages(),
    python_requires='>=3.6',
)
